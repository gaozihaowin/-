<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daily.dailychineseculture.mapper.CampPlanMapper">
    
    <!-- 结果映射：排课计划 DTO -->
    <resultMap id="CampPlanResultMap" type="com.daily.dailychineseculture.dto.CampPlanDTO">
        <result property="planId" column="plan_id"/>
        <result property="campId" column="camp_id"/>
        <result property="dayIndex" column="day_index"/>
        <result property="planDate" column="plan_date"/>
        <result property="title" column="title"/>
        <result property="videoUrl" column="video_url"/>
        <result property="graphicUrl" column="graphic_url"/>
    </resultMap>
    
    <!-- 根据营期 ID 查询排课计划列表 -->
    <select id="selectCampPlansByCampId" resultMap="CampPlanResultMap">
        SELECT 
            plan_id,
            camp_id,
            day_index,
            plan_date,
            title,
            video_url,
            graphic_url
        FROM t_camp_plan
        WHERE camp_id = #{campId}
        ORDER BY day_index ASC
    </select>
    
    <!-- 检查营期是否已有排课计划 -->
    <select id="countCampPlansByCampId" resultType="int">
        SELECT COUNT(*)
        FROM t_camp_plan
        WHERE camp_id = #{campId}
    </select>
    
    <!-- 批量插入排课计划 -->
    <insert id="batchInsertCampPlans" parameterType="java.util.List">
        INSERT INTO t_camp_plan (camp_id, day_index, plan_date, title, video_url, graphic_url)
        VALUES
        <foreach collection="list" item="plan" separator=",">
            (#{plan.campId}, #{plan.dayIndex}, #{plan.planDate}, #{plan.title}, #{plan.videoUrl}, #{plan.graphicUrl})
        </foreach>
    </insert>
    
    <!-- 根据 ID 更新排课计划 -->
    <update id="updateCampPlan" parameterType="com.daily.dailychineseculture.dto.CampPlanDTO">
        UPDATE t_camp_plan
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="videoUrl != null">
                video_url = #{videoUrl},
            </if>
            <if test="graphicUrl != null">
                graphic_url = #{graphicUrl},
            </if>
        </set>
        WHERE plan_id = #{planId}
    </update>
    
    <!-- 根据营期 ID 查询课程安排（按天排序） -->
    <select id="selectCourseScheduleByCampId" resultType="com.daily.dailychineseculture.entity.CampPlan">
        SELECT 
            plan_id,
            camp_id,
            day_index,
            plan_date,
            title,
            video_url,
            graphic_url,
            module_index,
            module_name,
           reading_title,
            teacher_name,
            video_duration
        FROM t_camp_plan
        WHERE camp_id = #{campId}
        ORDER BY day_index ASC
    </select>
    
</mapper>
