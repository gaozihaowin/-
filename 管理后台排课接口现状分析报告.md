# 管理后台排课接口现状分析报告

## 文档概述

本文档对管理后台"教务排课"功能的后端实现进行逆向梳理，分析当前排课数据的存储机制，并评估与未来一对多架构的兼容性。

**更新记录**：
- 2026-03-21：完成一对多架构重构，支持任务管理

---

## 1. 数据库实体层分析

### 1.1 CampPlan 实体类

**文件路径**：`src/main/java/com/daily/dailychineseculture/entity/CampPlan.java`

```java
package com.daily.dailychineseculture.entity;

import lombok.Data;
import org.apache.ibatis.type.Alias;
import java.util.Date;

/**
 * 排课计划实体类
 * 对应数据库表：t_camp_plan
 */
@Data
@Alias("CampPlan")
public class CampPlan {
    /**
     * 计划 ID
     */
    private Integer planId;
    
    /**
     * 营期 ID
     */
    private Integer campId;
    
    /**
     * 第几天
     */
    private Integer dayIndex;
    
    /**
     * 具体日期
     */
    private Date planDate;
    
    /**
     * 导读标题
     */
    private String title;
    
    /**
     * 模块索引（第几周）
     */
    private Integer moduleIndex;
    
    /**
     * 模块名称
     */
    private String moduleName;
    
    /**
     * 讲师姓名
     */
    private String teacherName;
    
    /**
     * 是否完成：0-未完成，1-已完成
     */
    private Integer isFinished;
}
```

### 1.2 关键发现：扁平化字段已移除

| 字段名 | 是否存在 | 说明 |
|--------|----------|------|
| `videoUrl` | ❌ 不存在 | 原视频链接字段已被移除 |
| `graphicUrl` | ❌ 不存在 | 原图文链接字段已被移除 |
| `readingUrl` | ❌ 不存在 | 原阅读链接字段已被移除 |
| `title` | ✅ 存在 | 导读标题 |
| `teacherName` | ✅ 存在 | 讲师姓名 |
| `moduleIndex` | ✅ 存在 | 模块索引 |
| `moduleName` | ✅ 存在 | 模块名称 |

**结论**：`CampPlan` 实体类中**不包含** `videoUrl`、`graphicUrl` 等扁平化字段。视频和图文链接已迁移至 `t_plan_task` 任务表。

### 1.3 t_plan_task 任务表结构

**Mapper 文件路径**：`src/main/resources/mapper/PlanTaskMapper.xml`

根据 SQL 查询推断，`t_plan_task` 表包含以下字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `task_id` | Integer | 任务 ID（主键） |
| `plan_id` | Integer | 关联的排课计划 ID（外键） |
| `task_type` | String | 任务类型（VIDEO, READ, HOMEWORK） |
| `task_name` | String | 任务名称/标题 |
| `task_desc` | String | 任务描述/副标题 |
| `task_url` | String | 任务链接（视频/图文 URL） |
| `is_required` | Integer | 是否必做：1-必做，0-选做 |
| `sort_order` | Integer | 排序序号 |
| `is_deleted` | Integer | 逻辑删除标记：0-正常，1-已删除 |

**关联关系**：

```
┌─────────────────┐         ┌─────────────────┐
│  t_camp_plan    │  1 ── N │  t_plan_task    │
├─────────────────┤         ├─────────────────┤
│ plan_id (PK)    │◄────────│ plan_id (FK)    │
│ camp_id         │         │ task_id (PK)    │
│ day_index       │         │ task_type       │
│ plan_date       │         │ task_name       │
│ title           │         │ task_desc       │
│ ...             │         │ task_url        │
└─────────────────┘         │ is_required     │
                            │ sort_order      │
                            │ is_deleted      │
                            └─────────────────┘
```

---

## 2. Controller 与接口定义

### 2.1 CampPlanController（已重构）

**文件路径**：`src/main/java/com/daily/dailychineseculture/controller/CampPlanController.java`

```java
package com.daily.dailychineseculture.controller;

import com.daily.dailychineseculture.common.ResponseResult;
import com.daily.dailychineseculture.dto.CampOptionDTO;
import com.daily.dailychineseculture.dto.CampPlanDTO;
import com.daily.dailychineseculture.dto.GenerateCalendarRequest;
import com.daily.dailychineseculture.service.CampPlanService;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/**
 * 教务排课工作台 Controller
 * 支持排课计划的 CRUD 操作，以及任务的一对多管理
 */
@RestController
@RequestMapping("/api/admin/camp-plans")
@RequiredArgsConstructor
public class CampPlanController {

    private final CampPlanService campPlanService;

    /**
     * 获取某营期的排课时间轴
     * GET /api/admin/camp-plans?campId={campId}
     *
     * 每个排课计划会包含其下的所有任务列表
     *
     * @param campId 营期 ID
     * @return 统一响应结果，包含排课计划列表（含任务）
     */
    @GetMapping
    public ResponseResult<List<CampPlanDTO>> getCampPlans(@RequestParam Integer campId) {
        List<CampPlanDTO> plans = campPlanService.getCampPlansByCampId(campId);
        return ResponseResult.success("查询成功", plans);
    }

    /**
     * 一键生成空日历
     * POST /api/admin/camp-plans/generate
     *
     * @param request 生成日历请求
     * @return 统一响应结果
     */
    @PostMapping("/generate")
    public ResponseResult<String> generateCalendar(@RequestBody GenerateCalendarRequest request) {
        campPlanService.generateCalendar(request);
        return ResponseResult.success("日历框架生成成功");
    }

    /**
     * 新增一天的排课
     * POST /api/admin/camp-plans
     *
     * @param campPlan 排课计划 DTO（包含 campId, dayIndex, planDate 等基本信息）
     * @return 统一响应结果，包含新增后的排课计划（含 planId）
     */
    @PostMapping
    public ResponseResult<CampPlanDTO> addCampPlan(@RequestBody CampPlanDTO campPlan) {
        CampPlanDTO result = campPlanService.addCampPlan(campPlan);
        return ResponseResult.success("新增成功", result);
    }

    /**
     * 保存/更新单日课表
     * PUT /api/admin/camp-plans
     *
     * 包括更新排课基本信息和全量同步任务列表
     *
     * @param campPlan 排课计划 DTO（包含 planId, title, tasks 等）
     * @return 统一响应结果
     */
    @PutMapping
    public ResponseResult<String> saveOrUpdateCampPlan(@RequestBody CampPlanDTO campPlan) {
        campPlanService.saveOrUpdateCampPlan(campPlan);
        return ResponseResult.success("保存成功");
    }

    /**
     * 删除整天排课及挂载的所有任务
     * DELETE /api/admin/camp-plans/{planId}
     *
     * @param planId 排课 ID
     * @return 统一响应结果
     */
    @DeleteMapping("/{planId}")
    public ResponseResult<String> deleteCampPlan(@PathVariable Integer planId) {
        campPlanService.deleteCampPlan(planId);
        return ResponseResult.success("删除成功");
    }
}
```

### 2.2 接口清单（已更新）

| HTTP Method | 接口路径 | 功能描述 | 状态 |
|-------------|----------|----------|------|
| `GET` | `/api/admin/camp-plans?campId={campId}` | 获取排课列表（含任务） | ✅ 已升级 |
| `POST` | `/api/admin/camp-plans/generate` | 一键生成空日历 | 原有 |
| `POST` | `/api/admin/camp-plans` | **新增**一天的排课 | ✅ 新增 |
| `PUT` | `/api/admin/camp-plans` | 保存单日课表（含任务全量同步） | ✅ 已升级 |
| `DELETE` | `/api/admin/camp-plans/{planId}` | **删除**整天排课及任务 | ✅ 新增 |

### 2.3 CampPlanDTO 结构（已更新）

**文件路径**：`src/main/java/com/daily/dailychineseculture/dto/CampPlanDTO.java`

```java
package com.daily.dailychineseculture.dto;

import lombok.Data;
import java.util.Date;
import java.util.List;

/**
 * 排课计划 DTO
 * 用于管理后台排课数据的传输
 */
@Data
public class CampPlanDTO {
    /**
     * 计划 ID
     */
    private Integer planId;

    /**
     * 营期 ID
     */
    private Integer campId;

    /**
     * 第几天
     */
    private Integer dayIndex;

    /**
     * 具体日期
     */
    private Date planDate;

    /**
     * 导读标题
     */
    private String title;

    /**
     * 任务列表
     * 该排课下的所有任务（视频、阅读、作业等）
     */
    private List<TaskAdminDTO> tasks;
}
```

### 2.4 TaskAdminDTO 结构（新增）

**文件路径**：`src/main/java/com/daily/dailychineseculture/dto/TaskAdminDTO.java`

```java
package com.daily.dailychineseculture.dto;

import lombok.Data;

/**
 * 任务管理 DTO
 * 用于管理后台排课时的任务数据传输
 */
@Data
public class TaskAdminDTO {
    /**
     * 任务 ID
     * 如果是前端新建的任务，此字段为 null
     */
    private Integer taskId;

    /**
     * 任务类型
     * VIDEO - 视频
     * READ - 阅读
     * HOMEWORK - 作业
     */
    private String taskType;

    /**
     * 任务名称
     */
    private String taskName;

    /**
     * 任务描述
     * 作业的描述或阅读的说明存这里
     */
    private String taskDesc;

    /**
     * 任务链接
     * 视频或阅读的链接
     */
    private String taskUrl;

    /**
     * 是否必做
     * 1 - 必修
     * 0 - 选修
     */
    private Integer isRequired;

    /**
     * 排序序号
     */
    private Integer sortOrder;
}
```

---

## 3. Service 层保存逻辑（已重构）

### 3.1 CampPlanServiceImpl

**文件路径**：`src/main/java/com/daily/dailychineseculture/service/impl/CampPlanServiceImpl.java`

```java
/**
 * 保存/更新单日课表
 * 包括更新排课基本信息和全量同步任务列表
 */
@Override
@Transactional(rollbackFor = Exception.class)
public void saveOrUpdateCampPlan(CampPlanDTO campPlan) {
    Integer planId = campPlan.getPlanId();

    // 1. 更新排课基本信息
    campPlanMapper.updateCampPlan(campPlan);

    // 2. 全量同步任务列表
    if (campPlan.getTasks() != null) {
        syncTasks(planId, campPlan.getTasks());
    }
}

/**
 * 全量同步任务列表
 * 核心逻辑：
 * - 前端传了 taskId 且数据库有的，执行 update
 * - 前端传的 taskId 为 null 的，执行 insert
 * - 数据库里原有，但前端没传的 taskId，执行 delete
 *
 * @param planId 排课 ID
 * @param newTasks 前端传来的任务列表
 */
private void syncTasks(Integer planId, List<TaskAdminDTO> newTasks) {
    // 1. 查询数据库中该 planId 原有的所有任务 ID
    List<Integer> existingTaskIds = planTaskMapper.selectTaskIdsByPlanId(planId);
    Set<Integer> existingTaskIdSet = existingTaskIds.stream().collect(Collectors.toSet());

    // 2. 收集前端传来的有效 taskId
    Set<Integer> newTaskIdSet = newTasks.stream()
            .map(TaskAdminDTO::getTaskId)
            .filter(id -> id != null)
            .collect(Collectors.toSet());

    // 3. 找出需要删除的任务 ID（数据库有，前端没有）
    List<Integer> toDeleteTaskIds = existingTaskIds.stream()
            .filter(id -> !newTaskIdSet.contains(id))
            .collect(Collectors.toList());

    // 4. 批量删除不再需要的任务
    if (!toDeleteTaskIds.isEmpty()) {
        planTaskMapper.deleteByTaskIds(toDeleteTaskIds);
    }

    // 5. 遍历前端传来的任务，执行 insert 或 update
    for (TaskAdminDTO taskDTO : newTasks) {
        if (taskDTO.getTaskId() == null) {
            // 新增任务
            PlanTask newTask = convertToEntity(planId, taskDTO);
            planTaskMapper.insertTask(newTask);
            // 回填 taskId（如果前端需要）
            taskDTO.setTaskId(newTask.getTaskId());
        } else {
            // 更新任务
            PlanTask updateTask = convertToEntity(planId, taskDTO);
            planTaskMapper.updateTask(updateTask);
        }
    }
}
```

### 3.2 保存逻辑分析（已升级）

| 步骤 | 操作 | 涉及表 |
|------|------|--------|
| 1 | 接收 `CampPlanDTO`（含 tasks 列表） | - |
| 2 | 调用 `campPlanMapper.updateCampPlan()` | `t_camp_plan` |
| 3 | 全量同步任务列表 | `t_plan_task` |
| 3.1 | 查询数据库中原有任务 ID | `t_plan_task` |
| 3.2 | 比对找出需要删除的任务 ID | - |
| 3.3 | 批量删除不再需要的任务 | `t_plan_task` |
| 3.4 | 遍历任务：taskId == null → insert | `t_plan_task` |
| 3.5 | 遍历任务：taskId != null → update | `t_plan_task` |

---

## 4. 重构完成总结

### 4.1 架构升级对比

| 维度 | 重构前 | 重构后 |
|------|--------|--------|
| **数据存储** | 视频链接已迁移至 `t_plan_task` | ✅ 一对多架构完整落地 |
| **管理接口** | 只能更新 `title` 字段 | ✅ 支持任务 CRUD |
| **前端交互** | 无任务管理界面 | ✅ 可填写视频、图文链接 |
| **API 设计** | 单一 PUT 接口 | ✅ 完整 CRUD 接口 |
| **任务同步** | 不支持 | ✅ 全量同步机制 |

### 4.2 数据契约示例

**GET/PUT 请求/响应格式**：

```json
{
  "planId": 123,
  "campId": 1,
  "dayIndex": 1,
  "planDate": "2026-06-20",
  "title": "第一课：阳明心学导论",
  "tasks": [
    {
      "taskId": 1001,
      "taskType": "VIDEO",
      "taskName": "导读视频",
      "taskDesc": "请认真观看",
      "taskUrl": "http://localhost:8080/uploads/xxx.mp4",
      "isRequired": 1,
      "sortOrder": 1
    },
    {
      "taskId": null,
      "taskType": "READ",
      "taskName": "阅读材料",
      "taskDesc": "请阅读以下内容",
      "taskUrl": "http://localhost:8080/uploads/reading.pdf",
      "isRequired": 1,
      "sortOrder": 2
    }
  ]
}
```

### 4.3 数据流现状图（已更新）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           管理后台前端                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  排课页面                                                        │   │
│  │  - 选择营期                                                      │   │
│  │  - 查看排课时间轴                                                │   │
│  │  - 编辑单日课表                                                  │   │
│  │    - 填写导读标题 ✅                                             │   │
│  │    - 填写视频链接 ✅                                             │   │
│  │    - 填写图文链接 ✅                                             │   │
│  │    - 添加/删除任务 ✅                                            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           后端 API                                      │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  GET    /api/admin/camp-plans?campId={campId}  ← 含任务列表     │   │
│  │  POST   /api/admin/camp-plans/generate                          │   │
│  │  POST   /api/admin/camp-plans  ← 新增排课                       │   │
│  │  PUT    /api/admin/camp-plans  ← 全量同步任务                   │   │
│  │  DELETE /api/admin/camp-plans/{planId} ← 删除排课及任务         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ✅ 已完成：任务管理 API                                                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           数据库层                                      │
│  ┌─────────────────────┐         ┌─────────────────────┐              │
│  │    t_camp_plan      │         │    t_plan_task      │              │
│  ├─────────────────────┤         ├─────────────────────┤              │
│  │ plan_id (PK)        │◄────────│ plan_id (FK)        │              │
│  │ camp_id             │    1:N  │ task_id (PK)        │              │
│  │ day_index           │         │ task_type           │              │
│  │ plan_date           │         │ task_name           │              │
│  │ title ✅            │         │ task_desc           │              │
│  │ module_index        │         │ task_url ✅         │              │
│  │ module_name         │         │ is_required         │              │
│  │ teacher_name        │         │ sort_order          │              │
│  │ is_finished         │         │ is_deleted          │              │
│  └─────────────────────┘         └─────────────────────┘              │
│                                                                         │
│  ✅ 数据库已完成范式化重构                                               │
│  ✅ 管理后台 API 已适配一对多架构                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 关键文件清单

| 文件 | 路径 | 作用 | 状态 |
|------|------|------|------|
| CampPlan.java | `src/main/java/.../entity/CampPlan.java` | 排课计划实体类 | 原有 |
| PlanTask.java | `src/main/java/.../entity/PlanTask.java` | 任务实体类 | ✅ 新增 |
| CampPlanDTO.java | `src/main/java/.../dto/CampPlanDTO.java` | 排课计划 DTO | ✅ 已更新 |
| TaskAdminDTO.java | `src/main/java/.../dto/TaskAdminDTO.java` | 任务管理 DTO | ✅ 新增 |
| CampPlanController.java | `src/main/java/.../controller/CampPlanController.java` | 管理后台排课 API | ✅ 已更新 |
| CampPlanService.java | `src/main/java/.../service/CampPlanService.java` | 排课服务接口 | ✅ 已更新 |
| CampPlanServiceImpl.java | `src/main/java/.../service/impl/CampPlanServiceImpl.java` | 排课服务实现 | ✅ 已重构 |
| CampPlanMapper.java | `src/main/java/.../mapper/CampPlanMapper.java` | 排课数据访问接口 | ✅ 已更新 |
| CampPlanMapper.xml | `src/main/resources/mapper/CampPlanMapper.xml` | 排课 SQL 映射 | ✅ 已更新 |
| PlanTaskMapper.java | `src/main/java/.../mapper/PlanTaskMapper.java` | 任务数据访问接口 | ✅ 已更新 |
| PlanTaskMapper.xml | `src/main/resources/mapper/PlanTaskMapper.xml` | 任务 SQL 映射 | ✅ 已更新 |

---

## 6. 现状总结

| 维度 | 现状 | 说明 |
|------|------|------|
| **数据库设计** | ✅ 已完成范式化 | `t_camp_plan` 一对多关联 `t_plan_task` |
| **实体类** | ✅ 已适配 | `CampPlan` 不再包含 `videoUrl` 等字段 |
| **C 端查询** | ✅ 已实现 | 可通过 `t_plan_task` 查询任务列表 |
| **管理后台 API** | ✅ 已完成 | 支持任务 CRUD 和全量同步 |
| **任务管理接口** | ✅ 已实现 | 通过 PUT 接口全量同步任务 |
| **排课天数管理** | ✅ 已实现 | 支持新增和删除排课天数 |
