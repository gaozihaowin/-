# 课件选择器 — 后端代码链路梳理

> 整理目的：支撑前端"选择课件资源"弹窗重构（左侧分类树 + 右侧分类列表 + 顶部关键字搜索），确保前后端联调对齐。
> 整理时间：2026-04-10

---

## 1. 课件分页列表查询（Controller → DTO → Mapper → XML）

### 1.1 Controller 层

**文件：** `CourseMaterialController.java`

```java
/**
 * 分页查询课件
 * GET /api/admin/materials/page
 */
@GetMapping("/page")
public ResponseResult<CourseMaterialPageResultDTO> getMaterialPage(CourseMaterialPageDTO pageDTO) {
    CourseMaterialPageResultDTO result = courseMaterialService.getMaterialPage(pageDTO);
    return ResponseResult.success(result);
}
```

| 项目 | 值 |
|------|-----|
| 接口路径 | `GET /api/admin/materials/page` |
| 返回格式 | `ResponseResult<CourseMaterialPageResultDTO>` |

---

### 1.2 请求参数 DTO

**文件：** `CourseMaterialPageDTO.java`

```java
@Data
public class CourseMaterialPageDTO {

    /** 分类 ID（支持树形联动，选中父分类时查所有子分类） */
    private Long categoryId;

    /** 类型: READ(阅读/文档), VIDEO(视频), AUDIO(音频) */
    private String type;

    /** 关键字（模糊匹配 name 字段） */
    private String keyword;

    /** 页码（默认1） */
    private Integer pageNum;

    /** 每页大小（默认10） */
    private Integer pageSize;
}
```

**✅ DTO 三大核心参数齐备：** `categoryId` | `type` | `keyword`

---

### 1.3 Mapper 接口

**文件：** `CourseMaterialMapper.java`

```java
List<CourseMaterial> selectPage(@Param("categoryIds") List<Long> categoryIds,
                                @Param("type") String type,
                                @Param("keyword") String keyword);

Integer countPage(@Param("categoryIds") List<Long> categoryIds,
                  @Param("type") String type,
                  @Param("keyword") String keyword);
```

> ⚠️ 注意：入参是 `List<Long> categoryIds`（复数），而非单个 `categoryId`。这是因为 Service 层在分类ID不为空时，会递归查出该分类及所有子分类的 ID 列表，一次性传给 Mapper。

---

### 1.4 Mapper XML SQL

**文件：** `CourseMaterialMapper.xml`

```xml
<!-- 分页查询课件（手写动态SQL） -->
<select id="selectPage" resultType="com.daily.dailychineseculture.entity.CourseMaterial">
    SELECT
        t.material_id   AS materialId,
        t.category_id   AS categoryId,
        mc.name         AS categoryName,   <!-- ✅ 联表查分类名 -->
        t.name,
        t.type,
        t.url,
        t.size,
        t.duration,
        t.create_by     AS createBy,
        t.create_time   AS createTime,
        t.update_time   AS updateTime
    FROM t_course_material t
    LEFT JOIN t_material_category mc ON t.category_id = mc.category_id
    <where>
        <!-- ✅ 支持 categoryIds（父分类+所有子孙分类的ID列表） -->
        <if test="categoryIds != null and categoryIds.size() > 0">
            AND t.category_id IN
            <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        <!-- ✅ 支持 type 精确匹配 -->
        <if test="type != null and type != ''">
            AND t.type = #{type}
        </if>
        <!-- ✅ 支持 keyword 模糊查询（name 字段） -->
        <if test="keyword != null and keyword != ''">
            AND t.name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </where>
    ORDER BY t.material_id DESC
</select>

<!-- 统计总数（用于分页） -->
<select id="countPage" resultType="int">
    SELECT COUNT(*)
    FROM t_course_material t
    LEFT JOIN t_material_category mc ON t.category_id = mc.category_id
    <where>
        <!-- ✅ 同上：categoryIds IN 列表 -->
        <if test="categoryIds != null and categoryIds.size() > 0">
            AND t.category_id IN
            <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        <!-- ✅ 同上：type 精确匹配 -->
        <if test="type != null and type != ''">
            AND t.type = #{type}
        </if>
        <!-- ✅ 同上：keyword LIKE 模糊查询 -->
        <if test="keyword != null and keyword != ''">
            AND t.name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </where>
</select>
```

**SQL 三大过滤条件现状：**

| 过滤条件 | SQL 实现 | 状态 |
|---------|---------|------|
| `categoryIds`（父分类+子孙） | `t.category_id IN (...)` | ✅ 已支持 |
| `type` 精确匹配 | `t.type = #{type}` | ✅ 已支持 |
| `keyword` 模糊搜索 | `t.name LIKE CONCAT('%', #{keyword}, '%')` | ✅ 已支持 |

---

### 1.5 Service 层递归分类 ID 收集逻辑

**文件：** `CourseMaterialServiceImpl.java` → `getMaterialPage()` 方法

```java
@Override
public CourseMaterialPageResultDTO getMaterialPage(CourseMaterialPageDTO pageDTO) {
    // 参数默认值处理
    if (pageDTO.getPageNum() == null || pageDTO.getPageNum() < 1) {
        pageDTO.setPageNum(1);
    }
    if (pageDTO.getPageSize() == null || pageDTO.getPageSize() < 1) {
        pageDTO.setPageSize(10);
    }

    // ✅ 递归收集该分类及所有子孙分类的 ID 列表
    List<Long> categoryIds = null;
    if (pageDTO.getCategoryId() != null) {
        List<MaterialCategory> all = materialCategoryMapper.selectAll();
        Map<Long, List<Long>> parentIdMap = all.stream()
                .collect(Collectors.groupingBy(
                        MaterialCategory::getParentId,
                        Collectors.mapping(MaterialCategory::getCategoryId, Collectors.toList())
                ));
        List<Long> resultIds = new ArrayList<>();
        resultIds.add(pageDTO.getCategoryId());
        findChildrenIds(pageDTO.getCategoryId(), parentIdMap, resultIds);
        categoryIds = resultIds;
    }

    // ✅ 分页查询
    PageHelper.startPage(pageDTO.getPageNum(), pageDTO.getPageSize());
    List<CourseMaterial> list = courseMaterialMapper.selectPage(
            categoryIds,
            pageDTO.getType(),
            pageDTO.getKeyword()
    );
    PageInfo<CourseMaterial> pageInfo = new PageInfo<>(list);

    // 组装返回
    CourseMaterialPageResultDTO result = new CourseMaterialPageResultDTO();
    result.setTotal(pageInfo.getTotal());
    result.setList(pageInfo.getList());
    return result;
}

/** 递归收集所有子孙分类 ID（内存中遍历，无递归查询） */
private void findChildrenIds(Long parentId, Map<Long, List<Long>> parentIdMap, List<Long> resultIds) {
    List<Long> children = parentIdMap.get(parentId);
    if (children != null) {
        for (Long childId : children) {
            resultIds.add(childId);
            findChildrenIds(childId, parentIdMap, resultIds);
        }
    }
}
```

**递归查询流程：**
```
前端传来 categoryId = 1（阳明心学）
  ↓
一次性查询所有分类 → 构建 parentIdMap
  ↓
递归 findChildrenIds(1)
  → [1, 5, 6, 7, ...]  (1 的所有子孙)
  ↓
SQL: WHERE t.category_id IN (1, 5, 6, 7, ...)
```

---

### 1.6 返回结构

**文件：** `CourseMaterialPageResultDTO.java`

```java
@Data
public class CourseMaterialPageResultDTO {
    private Long   total;          // 总记录数
    private List<CourseMaterial> list;  // 当页数据
}
```

---

## 2. 课件分类树获取（Controller → Service → VO）

### 2.1 Controller 层

**文件：** `MaterialCategoryController.java`

```java
/**
 * 获取分类树
 * GET /api/admin/material-categories/tree
 */
@GetMapping("/tree")
public ResponseResult<List<MaterialCategoryTreeVO>> getCategoryTree() {
    List<MaterialCategoryTreeVO> tree = materialCategoryService.getCategoryTree();
    return ResponseResult.success(tree);
}
```

| 项目 | 值 |
|------|-----|
| 接口路径 | `GET /api/admin/material-categories/tree` |
| 返回格式 | `ResponseResult<List<MaterialCategoryTreeVO>>` |

---

### 2.2 VO 结构

**文件：** `MaterialCategoryTreeVO.java`

```java
@Data
public class MaterialCategoryTreeVO {
    private Long   categoryId;   // 节点唯一 ID（即 el-tree-select 的 value）
    private Long   parentId;     // 父分类 ID
    private String name;         // 分类名称（即 el-tree-select 的 label）
    private Integer sort;        // 排序号
    private List<MaterialCategoryTreeVO> children;  // 子节点列表
}
```

**✅ el-tree-select 字段映射：**

| el-tree-select 属性 | MaterialCategoryTreeVO 字段 | 状态 |
|---------------------|-----------------------------|------|
| `node-key` / `value` | `categoryId` | ✅ |
| `label` | `name` | ✅ |
| `children` | `children` | ✅ |
| `parentId`（内部关联） | `parentId` | ✅ |

---

### 2.3 Service 层树形组装逻辑

**文件：** `MaterialCategoryServiceImpl.java`

```java
@Override
public List<MaterialCategoryTreeVO> getCategoryTree() {
    // 1. 一次性查出所有分类（全量内存组装，无需递归 SQL）
    List<MaterialCategory> allCategories = materialCategoryMapper.selectAll();

    // 2. 按 parentId 分组，构建 Map（Key: 父ID, Value: 子分类列表）
    Map<Long, List<MaterialCategory>> parentIdMap = allCategories.stream()
            .collect(Collectors.groupingBy(MaterialCategory::getParentId));

    // 3. 过滤出根节点（parentId == 0）
    List<MaterialCategoryTreeVO> rootNodes = allCategories.stream()
            .filter(c -> c.getParentId() != null && c.getParentId() == 0)
            .map(this::convertToTreeVO)
            .collect(Collectors.toList());

    // 4. 递归组装子树
    for (MaterialCategoryTreeVO node : rootNodes) {
        buildChildren(node, parentIdMap);
    }
    return rootNodes;
}

/** 将实体转换为 VO */
private MaterialCategoryTreeVO convertToTreeVO(MaterialCategory category) {
    MaterialCategoryTreeVO vo = new MaterialCategoryTreeVO();
    vo.setCategoryId(category.getCategoryId());
    vo.setParentId(category.getParentId());
    vo.setName(category.getName());
    vo.setSort(category.getSort());
    vo.setChildren(new ArrayList<>());
    return vo;
}

/** 递归构建子节点（内存遍历，无 DB 递归查询） */
private void buildChildren(MaterialCategoryTreeVO node, Map<Long, List<MaterialCategory>> parentIdMap) {
    List<MaterialCategory> children = parentIdMap.get(node.getCategoryId());
    if (children == null || children.isEmpty()) {
        return;
    }
    List<MaterialCategoryTreeVO> childNodes = children.stream()
            .map(this::convertToTreeVO)
            .collect(Collectors.toList());
    for (MaterialCategoryTreeVO childNode : childNodes) {
        buildChildren(childNode, parentIdMap);
    }
    node.setChildren(childNodes);
}
```

**树构建流程：**
```
selectAll() 全量查出
    ↓
按 parentId 分组 → parentIdMap { 0: [1,2,3], 1: [5,6], 5: [9] }
    ↓
过滤 parentId == 0 → 根节点 [阳明心学, ...]
    ↓
递归 buildChildren() → 无限级树
```

---

### 2.4 底层 SQL

**文件：** `MaterialCategoryMapper.xml`

```xml
<select id="selectAll" resultType="com.daily.dailychineseculture.entity.MaterialCategory">
    SELECT
        category_id AS categoryId,
        parent_id   AS parentId,
        name,
        sort
    FROM t_material_category
    ORDER BY sort ASC, category_id ASC
</select>
```

---

## 3. 前端调用契约汇总

### 3.1 课件列表分页

```
GET /api/admin/materials/page?categoryId=1&type=VIDEO&keyword=阳明&pageNum=1&pageSize=10
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `categoryId` | Long | 分类ID（支持父分类→查所有子孙） |
| `type` | String | 课件类型：READ / VIDEO / AUDIO |
| `keyword` | String | 搜索关键字（模糊匹配 name） |
| `pageNum` | Integer | 页码（默认1） |
| `pageSize` | Integer | 每页条数（默认10） |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 42,
    "list": [
      {
        "materialId": 5,
        "categoryId": 1,
        "categoryName": "阳明心学",
        "name": "《传习录》精读视频",
        "type": "VIDEO",
        "url": "https://cdn.xxx/video.mp4",
        "duration": 1800,
        "size": 52428800
      }
    ]
  }
}
```

---

### 3.2 分类树

```
GET /api/admin/material-categories/tree
```

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {
      "categoryId": 1,
      "parentId": 0,
      "name": "阳明心学",
      "sort": 10,
      "children": [
        {
          "categoryId": 5,
          "parentId": 1,
          "name": "视频课程",
          "sort": 10,
          "children": []
        }
      ]
    }
  ]
}
```

---

## 4. 联调注意事项

### ⚠️ 分页参数名
前端如果使用 `page` / `size`，需要改为 `pageNum` / `pageSize`。

### ⚠️ 分类树根节点判断
Service 层使用 `parentId == 0` 作为根节点过滤条件。如果数据库中根节点的 `parent_id` 存的是 `null` 而非 `0`，这些节点会丢失。建议数据规范统一用 `0` 表示根节点。

### ⚠️ 分类递归死循环防护
`findChildrenIds` 使用 `parentIdMap` 做内存遍历，避免了递归 SQL。但需要确保 `parentIdMap` 中不存在自引用环（如 A.parentId = B 且 B.parentId = A），否则会死循环。
