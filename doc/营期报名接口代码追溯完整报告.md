# 营期报名接口代码追溯报告

---

## 第一部分：全局搜索排查

### 搜索关键词：`当前营期不可报名`

**搜索结果**：在整个 `src` 目录下搜索 `"当前营期不可报名"` 字符串，**无匹配结果**。

### 搜索关键词：`当前营期已结束`

**搜索结果**：在 `CampServiceImpl.java` 中找到 1 处：

| 文件路径 | 行号 | 代码 |
|---------|------|------|
| `src/main/java/com/daily/dailychineseculture/service/impl/CampServiceImpl.java` | 222 | `throw new IllegalArgumentException("当前营期已结束，不可报名");` |

**结论**：旧报错信息 `"当前营期不可报名"` 已被废弃，当前代码使用新的报错信息 `"当前营期已结束，不可报名"`。

---

## 第二部分：后端核心链路代码

### 2.1 CampController.java

**文件路径**：`src/main/java/com/daily/dailychineseculture/controller/CampController.java`

```java
package com.daily.dailychineseculture.controller;

import com.daily.dailychineseculture.dto.CampDTO;
import com.daily.dailychineseculture.dto.CampEnrollDTO;
import com.daily.dailychineseculture.entity.Camp;
import com.daily.dailychineseculture.service.CampService;
import com.daily.dailychineseculture.util.ResponseResult;
import jakarta.annotation.Resource;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/camp")
public class CampController {

    @Resource
    private CampService campService;

    /**
     * 营期报名接口
     * POST /camp/enroll
     *
     * @param dto 报名请求 DTO（包含 campId）
     * @param request HTTP 请求对象
     * @return 统一响应结果
     */
    @PostMapping("/enroll")
    public ResponseResult<Void> enrollCamp(@RequestBody CampEnrollDTO dto, HttpServletRequest request) {
        try {
            if (dto == null || dto.getCampId() == null) {
                return ResponseResult.error(400, "campId 不能为空");
            }
            Long userId = (Long) request.getAttribute("userId");
            if (userId == null) {
                return ResponseResult.error(401, "未登录或登录已过期");
            }
            campService.enrollCamp(userId, dto.getCampId());
            return ResponseResult.success("报名成功", null);
        } catch (IllegalArgumentException e) {
            return ResponseResult.error(400, e.getMessage());
        } catch (Exception e) {
            return ResponseResult.error("报名失败：" + e.getMessage());
        }
    }
}
```

### 2.2 CampServiceImpl.java - enrollCamp 方法

**文件路径**：`src/main/java/com/daily/dailychineseculture/service/impl/CampServiceImpl.java`

```java
@Override
@Transactional(rollbackFor = Exception.class)
public void enrollCamp(Long userId, Integer campId) {
    if (userId == null) {
        throw new IllegalArgumentException("用户未登录");
    }
    if (campId == null) {
        throw new IllegalArgumentException("campId 不能为空");
    }

    Camp camp = campMapper.selectCampForEnroll(campId);
    if (camp == null) {
        throw new IllegalArgumentException("营期不存在");
    }
    if (camp.getEndTime() != null && new Date().after(camp.getEndTime())) {
        throw new IllegalArgumentException("当前营期已结束，不可报名");
    }

    Integer count = campEnrollmentMapper.countByUserIdAndCampId(userId, campId);
    if (count != null && count > 0) {
        throw new IllegalArgumentException("您已报名过该营期，请勿重复操作");
    }

    try {
        int inserted = campEnrollmentMapper.insertEnrollment(userId, campId);
        if (inserted <= 0) {
            throw new RuntimeException("报名失败");
        }
    } catch (DuplicateKeyException e) {
        throw new IllegalArgumentException("您已报名过该营期，请勿重复操作");
    }

    int updated = campMapper.incrementEnrollCount(campId);
    if (updated <= 0) {
        throw new RuntimeException("更新报名人数失败");
    }
}
```

**关键逻辑说明**：
- **第 221-222 行**：使用 `new Date().after(camp.getEndTime())` 判断当前时间是否超过结营时间
- **逻辑**：只要 `endTime` 还没过（当前时间 <= endTime），就可以报名
- **不再使用废弃的 status 物理字段**

### 2.3 CampMapper.xml - selectCampForEnroll

**文件路径**：`src/main/resources/mapper/CampMapper.xml`

```xml
<select id="selectCampForEnroll" resultType="com.daily.dailychineseculture.entity.Camp">
    SELECT camp_id, start_time, end_time
    FROM t_camp
    WHERE camp_id = #{campId}
</select>

<update id="incrementEnrollCount">
    update t_camp
    set enroll_count = enroll_count + 1
    where camp_id = #{campId}
</update>
```

**SQL 说明**：
- **仅查询 `camp_id`, `start_time`, `end_time`**，不查询 `status` 字段
- **不使用 `CASE WHEN`** 计算虚拟 status
- 判定逻辑完全由 **Java 代码**（`new Date().after(camp.getEndTime())`）在 Service 层完成

### 2.4 CampEnrollmentMapper.java

**文件路径**：`src/main/java/com/daily/dailychineseculture/mapper/CampEnrollmentMapper.java`

```java
package com.daily.dailychineseculture.mapper;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Update;

@Mapper
public interface CampEnrollmentMapper {

    Integer countByUserIdAndCampId(@Param("userId") Long userId, @Param("campId") Integer campId);

    int insertEnrollment(@Param("userId") Long userId, @Param("campId") Integer campId);

    @Update("UPDATE t_camp_enrollment SET progress = #{progress} WHERE user_id = #{userId} AND camp_id = #{campId}")
    int updateProgress(@Param("userId") Long userId, @Param("campId") Integer campId, @Param("progress") Integer progress);
}
```

### 2.5 Camp.java - Entity 实体类

**文件路径**：`src/main/java/com/daily/dailychineseculture/entity/Camp.java`

```java
package com.daily.dailychineseculture.entity;

import lombok.Data;
import org.apache.ibatis.type.Alias;
import java.util.Date;

/**
 * 营期实体类
 * 对应数据库表: t_camp
 */
@Data
@Alias("Camp")
public class Camp {
    /**
     * 营期ID
     */
    private Integer campId;

    /**
     * 营期类型 ID
     */
    private Integer typeId;

    /**
     * 期数
     */
    private Integer term;

    /**
     * 营期名称
     */
    private String name;

    /**
     * 营期介绍
     */
    private String intro;

    /**
     * 开营时间
     */
    private Date startTime;

    /**
     * 结营时间
     */
    private Date endTime;

    /**
     * 状态：0 未开始，1 进行中，2 已结束
     * 注意：此字段已废弃，status 由 Java 代码根据 endTime 动态判定
     */
    private Integer status;

    /**
     * 标签
     */
    private String tag;

    /**
     * 报名人数
     */
    private Integer enrollCount;
}
```

### 2.6 CampEnrollDTO.java

**文件路径**：`src/main/java/com/daily/dailychineseculture/dto/CampEnrollDTO.java`

```java
package com.daily.dailychineseculture.dto;

import lombok.Data;

@Data
public class CampEnrollDTO {
    private Integer campId;
}
```

---

## 第三部分：报名链路数据流图

```
前端 POST /camp/enroll  { "campId": xxx }
           ↓
    CampController.enrollCamp()
           ↓
    解析 @RequestBody CampEnrollDTO
    从 request.getAttribute("userId") 获取 userId
           ↓
    CampServiceImpl.enrollCamp()
           ↓
    campMapper.selectCampForEnroll(campId)
    ┌─────────────────────────────────────┐
    │  SQL: SELECT camp_id, start_time,   │
    │       end_time                     │
    │  FROM t_camp WHERE camp_id = ?     │
    │  不查 status，不做 CASE WHEN       │
    └─────────────────────────────────────┘
           ↓
    返回 Camp 对象
           ↓
    if (camp == null) → "营期不存在" ❌
           ↓
    if (camp.getEndTime() != null && new Date().after(camp.getEndTime()))
    → "当前营期已结束，不可报名" ❌
           ↓
    campEnrollmentMapper.countByUserIdAndCampId(userId, campId)
    → count > 0 → "您已报名过该营期" ❌
           ↓
    campEnrollmentMapper.insertEnrollment(userId, campId)
           ↓
    campMapper.incrementEnrollCount(campId)
           ↓
    返回 "报名成功" ✅
```

---

## 第四部分：可能仍然报错的场景

根据当前代码分析，如果仍然收到 400 报错，可能的原因如下：

| 报错信息 | 原因 | 解决方案 |
|---------|------|---------|
| `"当前营期已结束，不可报名"` | `endTime` 确实已过（当前时间 > 结束时间） | 这是正常业务逻辑，营期结束后无法报名 |
| `"营期不存在"` | `campId` 对应的营期在数据库中不存在 | 检查前端传入的 `campId` 是否正确 |
| `"您已报名过该营期，请勿重复操作"` | 用户已经报名过该营期 | 这是正常业务逻辑 |
| `"报名失败：..."` | 数据库插入或更新失败 | 检查数据库连接和权限 |

**如果前端仍然收到 `"当前营期不可报名"` 的报错**：
- 说明后端代码**未重新编译部署**，仍在运行旧代码
- 需要重新执行 `mvn compile` 并重启应用

---

## 第五部分：验证步骤

1. **确认后端日志**：查看后端控制台日志，确认最新的报错信息是什么
2. **确认部署版本**：确认前端请求打到的是最新部署的后端服务
3. **提供 campId**：如果有报错，请提供具体的 `campId`，我可以帮你查数据库中该营期的 `start_time` 和 `end_time`