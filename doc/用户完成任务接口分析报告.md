# 用户完成任务接口分析报告

## 1. 接口基础信息

### API 路径
```
POST /courses/plan/{planId}/task/complete
```

### HTTP 请求方法
- **POST**

### 入参详情

#### 路径参数
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `planId` | Integer | 排课计划 ID |

#### 请求体 (@RequestBody)
```java
public class TaskCompleteReqDTO {
    private Integer taskId;  // 任务 ID
}
```

#### 隐式参数
| 参数名 | 来源 | 说明 |
|--------|------|------|
| `currentUserId` | HttpServletRequest.getAttribute("userId") | 从 Token 中解析的登录用户 ID |

---

## 2. Service 层业务逻辑

### 核心方法
`CourseServiceImpl.completeTask(Integer planId, TaskCompleteReqDTO req, Long currentUserId)`

### 业务流程

#### 步骤 1：参数校验
```java
if (req == null || req.getTaskId() == null) {
    throw new IllegalArgumentException("taskId 不能为空");
}
```

#### 步骤 2：验证排课计划存在性
```java
CampPlan plan = campPlanMapper.selectById(planId);
if (plan == null) {
    throw new IllegalArgumentException("排课计划不存在，planId: " + planId);
}
```

#### 步骤 3：验证任务归属关系
```java
Integer exists = planTaskMapper.countTaskInPlan(planId, taskId);
if (exists == null || exists <= 0) {
    throw new IllegalArgumentException("任务不存在或不属于该排课，taskId: " + taskId);
}
```

#### 步骤 4：记录用户打卡（核心操作）
```java
userTaskRecordMapper.upsertDoneRecord(currentUserId, planId, taskId);
```
> **重要说明**：当前实现已适配范式化重构，不再使用 `taskType` 字段判断更新哪个字段，而是直接通过 `taskId` 插入/更新 `t_user_task_record` 表。

#### 步骤 5：计算完成率
```java
// 查询该排课下必做任务总数
Integer totalRequired = planTaskMapper.countRequiredTasksByPlanId(planId);

// 查询用户已完成的必做任务数
Integer completedRequired = planTaskMapper.countCompletedRequiredTasksByUserIdAndPlanId(currentUserId, planId);

int total = totalRequired == null ? 0 : totalRequired;
int completed = completedRequired == null ? 0 : completedRequired;

// 计算完成率公式
int newRate = total > 0 ? (completed * 100) / total : 0;

// 判断是否全部完成
int isAllCompleted = newRate == 100 ? 1 : 0;
```

**完成率计算公式**：
```
completionRate = (已完成必做任务数 / 必做任务总数) × 100
```

#### 步骤 6：更新汇总表
```java
userDailyRecordMapper.upsertSummary(currentUserId, plan.getCampId(), planId, newRate, isAllCompleted);
```

#### 步骤 7：构建响应
```java
TaskCompleteRespDTO resp = new TaskCompleteRespDTO();
resp.setPlanId(planId);
resp.setTaskType(planTaskMapper.selectTaskTypeByTaskId(taskId));
resp.setCompletionRate(newRate);
return resp;
```

---

## 3. SQL 实现

### 3.1 记录用户打卡（UserTaskRecordMapper.xml）

```xml
<insert id="upsertDoneRecord">
    INSERT INTO t_user_task_record (user_id, plan_id, task_id, is_done)
    VALUES (#{userId}, #{planId}, #{taskId}, 1)
    ON DUPLICATE KEY UPDATE is_done = 1
</insert>
```

**说明**：使用 `INSERT ... ON DUPLICATE KEY UPDATE` 实现幂等打卡，避免重复记录。

---

### 3.2 统计必做任务总数（PlanTaskMapper.xml）

```xml
<select id="countRequiredTasksByPlanId" resultType="int">
    SELECT COUNT(*)
    FROM t_plan_task
    WHERE plan_id = #{planId}
      AND is_required = 1
      AND is_deleted = 0
</select>
```

---

### 3.3 统计已完成的必做任务数（PlanTaskMapper.xml）

```xml
<select id="countCompletedRequiredTasksByUserIdAndPlanId" resultType="int">
    SELECT COUNT(*)
    FROM t_plan_task t
    JOIN t_user_task_record r ON t.task_id = r.task_id
    WHERE t.plan_id = #{planId}
      AND t.is_required = 1
      AND t.is_deleted = 0
      AND r.user_id = #{userId}
      AND r.is_done = 1
</select>
```

---

### 3.4 更新每日汇总记录（UserDailyRecordMapper.java）

```java
@Insert("INSERT INTO t_user_daily_record(user_id, camp_id, plan_id, completion_rate, is_all_completed) " +
        "VALUES(#{userId}, #{campId}, #{planId}, #{completionRate}, #{isAllCompleted}) " +
        "ON DUPLICATE KEY UPDATE completion_rate = #{completionRate}, is_all_completed = #{isAllCompleted}")
int upsertSummary(@Param("userId") Long userId,
                  @Param("campId") Integer campId,
                  @Param("planId") Integer planId,
                  @Param("completionRate") Integer completionRate,
                  @Param("isAllCompleted") Integer isAllCompleted);
```

---

## 4. 架构变更对比

### 旧架构（宽表模式）
```
请求参数: { taskType: "READ" | "VIDEO" | "HOMEWORK" | "EXTRA1" | "EXTRA2" }
判断逻辑: switch(taskType) { case "READ": update is_read_done = 1; ... }
存储位置: t_user_daily_record 表中的多个状态字段
```

### 新架构（范式化模式）
```
请求参数: { taskId: Integer }
判断逻辑: 无需判断类型，直接通过 taskId 插入 t_user_task_record
存储位置: t_user_task_record 表（一条记录代表一个任务完成状态）
汇总计算: 通过 JOIN 查询动态计算完成率
```

---

## 5. 数据表关系

```
t_camp_plan (排课计划)
    ↓ 1:N
t_plan_task (任务定义)
    ↓ 1:N
t_user_task_record (用户打卡记录)
    ↓ 聚合
t_user_daily_record (每日汇总)
```

---

## 6. 关键文件路径

| 文件 | 路径 |
|------|------|
| Controller | `src/main/java/com/daily/dailychineseculture/controller/AppCourseController.java` |
| Service | `src/main/java/com/daily/dailychineseculture/service/impl/CourseServiceImpl.java` |
| PlanTaskMapper | `src/main/java/com/daily/dailychineseculture/mapper/PlanTaskMapper.java` |
| UserTaskRecordMapper | `src/main/java/com/daily/dailychineseculture/mapper/UserTaskRecordMapper.java` |
| UserDailyRecordMapper | `src/main/java/com/daily/dailychineseculture/mapper/UserDailyRecordMapper.java` |
| PlanTaskMapper.xml | `src/main/resources/mapper/PlanTaskMapper.xml` |
| UserTaskRecordMapper.xml | `src/main/resources/mapper/UserTaskRecordMapper.xml` |
